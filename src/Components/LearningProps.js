
export default function Learning({firstName, lastName, email}){
    return(
      <div>
          <h2>First Name : {firstName}</h2>
          <h2>Last Name : {lastName}</h2>
          <h2>email : {email}</h2>
      </div>
    );
}